@model IEnumerable<WebApp.Models.KitapKategori>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Kitap)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kategori)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Kitap.KitapAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kategori.KategoriID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KitapKategoriID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.KitapKategoriID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.KitapKategoriID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
